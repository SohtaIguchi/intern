## 実行日時: 2026-08-25 15:34:56
**質問:** 平均的な年齢を教えてください

**実行SQL:**
```sql
SELECT AVG("平均年齢") FROM sales;
```

**回答:**
平均的な年齢は約42.3歳です。

今回のデータに基づくと、平均年齢は42.27歳となっています。これは一般的な成人の年齢層にあたり、若すぎず高齢でもない中年世代が中心であることを示しています。この平均年齢を参考に、対象集団の年齢構成を把握しやすくなるでしょう。

----------------------------------------

## 実行日時: 2026-08-25 15:35:39
**質問:** 年齢別に教えて

**実行SQL:**
```sql
SELECT
  年代,
  SUM("顧客数 (サービスUU)") AS 合計サービスUU,
  SUM("顧客数 (契約者UU)") AS 合計契約者UU,
  AVG(平均年齢) AS 平均年齢,
  AVG(平均契約日数) AS 平均契約日数,
  AVG(平均契約月数) AS 平均契約月数,
  AVG(平均現移動機利用月数) AS 平均現移動機利用月数,
  AVG(平均移動機変更累積回数) AS 平均移動機変更累積回数,
  AVG(平均家族利用回線数) AS 平均家族利用回線数,
  AVG(平均Web利用回数) AS 平均Web利用回数,
  AVG(平均来店回数) AS 平均来店回数,
  AVG(平均CS利用回数) AS 平均CS利用回数,
  AVG(平均EC利用回数) AS 平均EC利用回数
FROM sales
GROUP BY 年代
ORDER BY 年代;
```

**回答:**
年齢別のデータをまとめると、以下のようになります。

- 20代：契約者数は5人、平均年齢は約25.6歳、平均契約日数は約10.4日、平均契約月数は約0.67か月です。
- 30代：契約者数は11人、平均年齢は約34.9歳、平均契約日数は約12.1日、平均契約月数は約0.89か月です。
- 40代：契約者数は16人、平均年齢は約44.2歳、平均契約日数は約13.7日、平均契約月数は1か月です。
- 50代：契約者数は7人、平均年齢は約55.4歳、平均契約日数は約13.3日、平均契約月数は1か月です。
- 60代：契約者数は3人、平均年齢は約61.3歳、平均契約日数は8日、平均契約月数は1か月です。
- 70代：契約者数は8人、平均年齢は74.5歳、平均契約日数は約9.9日、平均契約月数は1か月です。
- 80代以上：契約者数は2人、平均年齢は82.5歳、平均契約日数は12日、平均契約月数は1か月です。
- 不明：契約者数は35人、平均契約日数は約4.5日、平均契約月数は約0.34か月です。

【傾向】
- 契約者数は40代が最も多く16人、次いで30代が11人、20代が5人となっています。
- 平均契約日数は40代と50代で13日を超え、他の年代よりやや長い傾向です。
- 平均契約月数は40代以降はほぼ1か月で安定していますが、20代～30代は1か月未満です。
- 60代以上は契約者数が少ないものの、平均契約月数は1か月で、契約期間は比較的安定しています。

以上より、20～50代の契約者が多く、特に40代は契約日数や契約月数ともに最も長い傾向が見られます。年齢が上がるにつれて契約期間は安定するものの、60代以降は契約者数が減少しています。

----------------------------------------

## 実行日時: 2026-08-25 15:40:18
**質問:** どんな項目がわかりますか

**実行SQL:**
```sql
SELECT 
  支社コード, 
  端末契約フラグ, 
  年代, 
  契約プラン, 
  サービス提供事業者, 
  BB固定契約状況, 
  BB固定internet種別, 
  ポイントサービス区分, 
  PayPay種別, 
  "顧客数 (サービスUU)", 
  "顧客数 (契約者UU)", 
  平均年齢, 
  平均契約日数, 
  平均契約月数, 
  平均現移動機利用月数, 
  平均移動機変更累積回数, 
  平均家族利用回線数, 
  平均Web利用回数, 
  平均来店回数, 
  平均CS利用回数, 
  平均EC利用回数 
FROM sales 
LIMIT 1;
```

**回答:**
このデータからは、お客様の基本属性や契約状況、利用行動に関する複数の項目がわかります。

具体的には、支社コードや年代、契約プランやサービス提供事業者といった属性情報のほか、端末契約や固定回線の契約状況、ポイントサービスやPayPayの利用区分などが含まれます。また、顧客数や平均年齢、平均契約日数・契約月数などの統計情報も確認できます。

数値例としては、平均年齢は48歳、平均契約日数は10日、平均契約月数は1ヶ月、平均EC利用回数は6回となっています。その他、平均現移動機利用月数や平均移動機変更累積回数は0であり、家族利用回線数やWeb利用回数、来店回数、CS利用回数も0と、利用頻度の傾向が読み取れます。

このように、顧客の属性や契約状況、各種サービスの利用状況を多角的に把握できるデータ構成になっています。

----------------------------------------

## 実行日時: 2026-08-25 15:57:38
**質問:** どんな情報が取れる？

**実行SQL:**
```sql
SELECT 
  イベントタイプ,
  トラックid,
  現在pv数,
  イベント発生日,
  イベント発生時間,
  デバイスid,
  事業者,
  システム名,
  ホスト名,
  訪問ページurl,
  ページ名,
  クエリ値,
  ハッシュ値,
  ネイティブ_platform,
  トラック誘導元page_url,
  ym,
  dt
FROM sales
LIMIT 100;
```

**回答:**
このデータから取得できる情報は、ウェブサイトやシステムにおけるユーザーのページ閲覧（ページビュー）に関する詳細なログ情報です。

具体的には以下のような情報が含まれています：

1. イベントタイプ：すべて「pageview」（ページ閲覧）イベントです。
2. トラックID：各ページビューに付与された一意の識別子。
3. 現在のページビュー数（現在pv数）：例えば1回や50回など、その時点でのページビュー数が数値で記録されています。
4. イベント発生日・時間：2026年8月12日（例）の日付と時間（HHMMSS形式）でイベント発生時刻がわかります。
5. デバイスID：ページ閲覧を行ったデバイスの識別子。
6. 事業者：SoftBank（SBM）、Y!Mobile（YM）、LINEMO（LINEMO）、SoftBank BB（SBB）、その他（UNK）などの事業者名。
7. システム名：UCS、OBI、Web情変、MenberSite、ソフトバンクHPなどの利用システム名。
8. ホスト名：アクセスされたホストのドメイン名。
9. 訪問ページURLおよびページ名：ユーザーが閲覧した具体的なページのURLとページ名。
10. クエリ値：URLパラメータとして付加されている情報（広告キャンペーン情報や識別子など）。
11. ハッシュ値：ページ内のアンカーや特定のセクションを示す場合があります。
12. ネイティブプラットフォーム：ユーザーのデバイスのOSやブラウザ環境（例：Linux armv81、iPhone、Win32、MacIntel など）。
13. トラック誘導元ページURL：どのページから遷移してきたかのURL。

【傾向・補足】
- 多様な事業者・システムからのアクセスを含み、特にSoftBank系のサービスが多く見られます。
- デバイスプラットフォームは主にLinux系やiPhone、Windows、Macintoshなど多岐にわたります。
- 訪問ページURLやクエリ値から、キャンペーンや特定のユーザー操作に関わるページ遷移を追跡可能です。
- イベント発生時間は細かく記録されており、ユーザー行動の時系列分析が可能です。
- エラーページへのアクセス（例：「/error/error」など）も記録されており、障害解析にも利用できます。

まとめると、このデータはユーザーのウェブサイト利用状況を詳細にトラッキングし、アクセス元、閲覧ページ、デバイス情報、時間帯などを把握できるため、ユーザー行動分析やサービス改善、マーケティング効果測定に活用可能です。

----------------------------------------

## 実行日時: 2026-08-25 16:02:16
**質問:** イベントタイプにはどんな値がありますか？

**実行SQL:**
```sql
SELECT DISTINCT イベントタイプ FROM sales;
```

**回答:**
イベントタイプには「pageview」が含まれています。ただし、取得データは最大転送件数の100,000件を超えたため、それ以上のイベントタイプの詳細は取得できていません。

つまり、「pageview」以外にも多様なイベントタイプが存在する可能性がありますが、現状のデータでは確認できていません。より詳細な情報を得るためには、データ取得件数の上限を引き上げるか、期間を分割して再度取得するなどの対応が必要です。

----------------------------------------

## 実行日時: 2026-08-25 16:03:51
**質問:** システム名の一覧とそれがいくつあるか教えて

**実行SQL:**
```sql
SELECT システム名, COUNT(*) AS システム数 FROM sales GROUP BY システム名;
```

**回答:**
システム名は全部で25種類あり、それぞれのシステム数の合計は約77,445件です。

具体的なシステム名と件数は以下の通りです（一部抜粋）：
- OBI：29,449件
- UCS：18,029件
- Y!Mobile HP：12,553件
- ソフトバンクHP：12,380件
- MenberSite：4,302件
- RES：3,663件
- SYD：3,358件
- MOB：3,353件
- UNK：2,630件
- CME：1,426件
- Web情変（2件あり合計約7,900件）

その他、件数の少ないシステムも含まれています。

補足として、「OBI」や「UCS」など一部のシステムは件数が非常に多く、全体の大部分を占めています。一方で、「CPS」や「WEB同意」など、件数が非常に少ないシステムも存在します。また、「Web情変」というシステム名が2回登場しており、合計すると約7,900件となります。これらのデータはシステムの利用頻度や規模の違いを示していると考えられます。

----------------------------------------

## 実行日時: 2026-08-25 16:31:48
**質問:** どんな情報がわかる？

**実行SQL:**
```sql
SELECT
  'pivot' AS テーブル名,
  GROUP_CONCAT(name) AS カラム名一覧
FROM pragma_table_info('pivot')
UNION ALL
SELECT
  'web_contact',
  GROUP_CONCAT(name)
FROM pragma_table_info('web_contact')
UNION ALL
SELECT
  'analyze',
  GROUP_CONCAT(name)
FROM pragma_table_info('analyze')
UNION ALL
SELECT
  'user_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('user_info')
UNION ALL
SELECT
  'contract_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('contract_info')
UNION ALL
SELECT
  'service_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('service_info')
UNION ALL
SELECT
  'survey_results',
  GROUP_CONCAT(name)
FROM pragma_table_info('survey_results')
UNION ALL
SELECT
  'medallia_survey',
  GROUP_CONCAT(name)
FROM pragma_table_info('medallia_survey')
UNION ALL
SELECT
  'customer_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('customer_info');
```

**回答:**
このデータベースからわかる情報は多岐にわたり、主に以下のカテゴリーに分類できます。

1. 顧客・契約情報
- 契約者の性別、年齢、職業、国籍、契約ステータス、契約期間（月数・日数）、契約プラン、支払方法、解約情報など詳細な契約履歴
- 地域別（都道府県、市区町村）や法人・個人の区分
- 契約者識別情報や家族利用状況、割引適用状況など

2. サービス・端末情報
- サービスIDやプラン、端末の種類、端末購入情報、利用状況（移動機利用月数、変更回数など）
- BB固定回線契約状況やインターネット種別、光回線やWi-Fi利用の有無
- ポイントサービス、PayPay種別などの付帯サービス情報

3. 利用行動・接触履歴
- Webサイトやアプリの利用イベント（ページビュー数、訪問ページURL、イベントタイプ、直近3ヶ月のWeb来店CS EC利用回数）
- 来店・カスタマーサービス利用回数
- お客様アンケートや調査結果（回答内容、回答日時、アンケート送信状況など）

4. 代理店・店舗情報
- 代理店コードや店舗コード、担当者情報、登録日時、代理店チャネル区分
- 申込時や解約時の代理店情報

5. その他
- 契約解除フラグ、盗難・紛失フラグ、滞納フラグなどリスク関連情報
- キャンペーンコードや特典適用情報
- 契約時の工事日や課金開始・終了日などのタイムライン情報

【補足】
- 契約期間は月数や日数で数値管理されており、顧客の平均年齢や平均契約月数などの統計データも参照可能です。
- 利用行動データと契約情報を組み合わせることで、顧客のサービス利用傾向やチャネル別の接触頻度を分析できます。
- アンケートや調査結果を活用して顧客満足度やニーズ把握も可能です。

まとめると、このデータベースは顧客の基本属性から契約・サービス利用状況、行動履歴、代理店情報、アンケート結果まで網羅的に管理しており、顧客分析、サービス改善、マーケティング施策立案に役立つ多角的な情報が得られます。

----------------------------------------

## 実行日時: 2026-08-25 16:33:05
**質問:** 平均年齢とかわかる？

**実行SQL:**
```sql
SELECT AVG("契約者年齢") AS 平均年齢 FROM analyze WHERE "契約者年齢" IS NOT NULL;
```

**回答:**
平均年齢は約429歳となっています。

この数値は通常の人間の年齢としては非常に高く、データの内容や測定対象に特異な条件がある可能性があります。例えば、年齢の単位が異なる、あるいは年齢以外の値が誤って平均年齢として計算されていることも考えられます。もし詳細なデータの背景や対象について分かれば、より正確な解釈が可能です。

----------------------------------------

## 実行日時: 2026-08-25 22:54:49
**質問:** どんな情報が見れる？

**実行SQL:**
```sql
SELECT 
  'pivot' AS テーブル名,
  GROUP_CONCAT(name) AS カラム名一覧
FROM pragma_table_info('pivot')
UNION ALL
SELECT 
  'web_contact',
  GROUP_CONCAT(name)
FROM pragma_table_info('web_contact')
UNION ALL
SELECT 
  'analyze',
  GROUP_CONCAT(name)
FROM pragma_table_info('analyze')
UNION ALL
SELECT 
  'user_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('user_info')
UNION ALL
SELECT 
  'contract_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('contract_info')
UNION ALL
SELECT 
  'service_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('service_info')
UNION ALL
SELECT 
  'survey_results',
  GROUP_CONCAT(name)
FROM pragma_table_info('survey_results')
UNION ALL
SELECT 
  'medallia_survey',
  GROUP_CONCAT(name)
FROM pragma_table_info('medallia_survey')
UNION ALL
SELECT 
  'customer_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('customer_info');
```

**回答:**
当データベースでは、以下のような多様な情報を閲覧できます。

1. 顧客・契約情報
- 支社コード、契約プラン、サービス提供事業者などの契約関連情報
- 契約者および使用者の性別・年齢・年代、職業、国籍情報
- 契約日数、契約月数、契約年数、契約状態、解約日や解約理由
- 契約者住所（都道府県、市区町村）、請求先情報
- 支払方法、割賦販売情報、キャンペーンコードなどの契約支払い関連情報
- 家族利用回線数や家族割引適用状況

2. サービス利用・行動データ
- Web利用回数、来店回数、CS（カスタマーサポート）利用回数、EC利用回数などの行動指標
- Webのイベントタイプ、訪問ページURL、ページ名、イベント発生日・時間などのトラッキングデータ
- ポイントサービス区分やPayPay種別などの決済・ポイント利用状況

3. 端末・プラン情報
- 端末契約フラグ、端末型番、IMEI番号、移動機利用月数や変更回数
- 現在および過去のプラン情報、プラン申込日・終了日

4. アンケート・顧客満足度データ
- アンケート回答内容（設問ごとの詳細な回答）、回答日時、送信状態
- アンケート送信・リマインド配信状況、担当者コードや店舗コードなどの業務関連

5. 店舗・代理店情報
- 代理店コード、店舗コード、店舗名、チャネル区分、代理店責任区分など

まとめると、顧客の基本属性から契約内容、サービス利用の行動履歴、端末情報、顧客満足度調査の回答結果まで幅広く確認可能です。これにより、顧客分析やサービス改善、マーケティング施策の評価など多角的な分析が行えます。

もし具体的に見たい情報や分析目的があればお知らせください。より詳細な項目や該当テーブル・カラムをご案内いたします。

----------------------------------------

## 実行日時: 2026-08-25 22:55:31
**質問:** どんな情報が見れるかテーブルごとに教えて

**実行SQL:**
```sql
SELECT 'pivot' AS テーブル名, 
       '顧客属性マトリクス集計データ。支社コード、端末契約フラグ、年代、契約プラン、サービス提供事業者、BB固定契約状況、BB固定internet種別、ポイントサービス区分、PayPay種別、顧客数(サービスUU)、顧客数(契約者UU)、平均年齢、平均契約日数、平均契約月数、平均現移動機利用月数、平均移動機変更累積回数、平均家族利用回線数、平均Web利用回数、平均来店回数、平均CS利用回数、平均EC利用回数が見れます。' AS 説明
UNION ALL
SELECT 'web_contact',
       'Webアクセス・PVログデータ。イベントタイプ、トラックid、現在pv数、イベント発生日、イベント発生時間、デバイスid、事業者、システム名、ホスト名、訪問ページurl、ページ名、クエリ値、ハッシュ値、ネイティブ_platform、トラック誘導元page_url、ym、dtが見れます。'
UNION ALL
SELECT 'analyze',
       '分析用属性データ。サービスid、登録年月、解約年月日、契約月数、契約年数、契約者性別、契約者年齢、使用者性別、使用者年齢、都道府県名、請求群id、支払方法、契約プラン、現在プラン開始日、前プラン解約日、基本パック、あんしん保証、光セット契約、3ヶ月以内web利用、3ヶ月以内店舗来店、3ヶ月以内cs利用、web_cnt_1m、shop_cnt_1m、call_cnt_1mが見れます。'
UNION ALL
SELECT 'user_info',
       '実利用者情報。サービスid、性別、年齢、ビジネス利用度、職業、更新年月日、データ入手ルートが見れます。'
UNION ALL
SELECT 'contract_info',
       '契約者情報。契約者id、契約者市区町村コード、性別コード、職業コード、契約者status_cd、契約者タイプコード、確認書タイプコード、契約タイプコード、申告国籍、国籍自動判定結果、設立年月が見れます。'
UNION ALL
SELECT 'service_info',
       'サービス情報。支社コード、端末契約フラグ、サービスid、データシェアgroup_id、ファミリーgroup_id、契約者id、請求グループid、使用者id、mvno識別、請求群id、サービスステータス、サービスタイプ、サービス区分、都道府県コード、市区町村コード、使用者年齢、契約プランid、契約プラン名が見れます。'
UNION ALL
SELECT 'survey_results',
       'アンケート実績データ。unique_id、サービスid、申込書番号、ginie受付番号、接客担当者コード、登録担当者コード、受付店コード、業務種別、申込区分、言語設定、アンケートid、アンケート回答日が見れます。'
UNION ALL
SELECT 'medallia_survey',
       'Medalliaアンケートデータ。アンケート通番、処理日、処理時間、ブランド、サービスid、店舗コード、店舗名、受付クルーコード、受付クルー名、アンケート回答内容が見れます。'
UNION ALL
SELECT 'customer_info',
       'BBカスタマ情報。カスタマid、契約者タイプコード、外国人フラグ、請求群id、性別、生年月、契約者年齢、契約者年代、サービス名称、都道府県、市区町村が見れます。'
```

**回答:**
ご覧いただける情報は、各テーブルごとに以下のように分類されています。

1. pivotテーブル  
　顧客属性マトリクス集計データで、支社コード、端末契約フラグ、年代、契約プラン、サービス提供事業者などの属性に加え、顧客数（サービスUU・契約者UU）、平均年齢、平均契約日数・月数、平均現移動機利用月数、平均移動機変更累積回数、平均家族利用回線数、平均Web・来店・CS・EC利用回数など、顧客の利用動向を総合的に把握できます。

2. web_contactテーブル  
　Webアクセス・PVログの詳細で、イベントタイプ、トラックID、現在のPV数、イベント発生日・時間、デバイスID、事業者、システム名、訪問ページURLやページ名、クエリ値など、ユーザーのWeb行動解析に適した情報が含まれます。

3. analyzeテーブル  
　分析用の属性データとして、サービスID、登録年月、解約年月日、契約年数・月数、契約者および使用者の性別・年齢、都道府県名、請求群ID、支払方法、契約プラン、基本パックやあんしん保証の有無、3ヶ月以内のWeb利用や店舗来店、CS利用状況、1ヶ月あたりのWeb・店舗・コール利用回数など、契約状況と利用履歴の分析が可能です。

4. user_infoテーブル  
　実利用者の基本情報として、サービスID、性別、年齢、ビジネス利用度、職業、更新年月日、データ入手ルートが含まれ、利用者属性の把握に役立ちます。

5. contract_infoテーブル  
　契約者の詳細情報で、契約者ID、市区町村コード、性別コード、職業コード、契約者ステータスコード、契約者タイプコード、確認書タイプコード、契約タイプコード、申告国籍、国籍自動判定結果、設立年月など、契約者の属性と契約状況を確認できます。

6. service_infoテーブル  
　サービスに関する情報として、支社コード、端末契約フラグ、サービスID、データシェアグループID、ファミリーグループID、契約者ID、請求グループID、使用者ID、MVNO識別、請求群ID、サービスステータス、サービスタイプ、サービス区分、都道府県コード、市区町村コード、使用者年齢、契約プランID・契約プラン名が含まれ、サービス提供状況の把握に利用できます。

7. survey_resultsテーブル  
　アンケート実績データとして、ユニークID、サービスID、申込書番号、受付番号、接客担当者コード、登録担当者コード、受付店コード、業務種別、申込区分、言語設定、アンケートID、アンケート回答日が見られ、アンケート回答の履歴管理に役立ちます。

8. medallia_surveyテーブル  
　Medalliaによるアンケートデータで、アンケート通番、処理日・時間、ブランド名、サービスID、店舗コード・店舗名、受付クルーコード・名、アンケート回答内容が含まれ、顧客満足度調査などに活用できます。

9. customer_infoテーブル  
　BBカスタマ情報として、カスタマID、契約者タイプコード、外国人フラグ、請求群ID、性別、生年月、契約者年齢・年代、サービス名称、都道府県、市区町村が含まれ、顧客の基本属性を把握できます。

これらのテーブルを組み合わせることで、顧客属性や契約情報、サービス利用状況、Web行動、アンケート結果など多角的に分析可能です。特にpivotテーブルは顧客の平均利用状況を一括で把握できるため、全体傾向の把握に便利です。

----------------------------------------

## 実行日時: 2026-08-25 23:03:55
**質問:** analyzeには何がある？

**実行SQL:**
```sql
SELECT DISTINCT name FROM pragma_table_info('analyze');
```

**回答:**
analyzeには主に以下のような項目があります。

1. 基本情報
- サービスid
- 登録年月
- 解約年月日
- 契約月数・契約年数
- 契約者性別・契約者年齢
- 使用者性別・使用者年齢
- 都道府県名

2. 支払・請求関連情報
- 請求群id
- 支払方法

3. 端末・チャネル関連情報
- 買換日_現在
- 端末_現在
- チャネル_現在
- 一次代理店_現在、取次店_現在
- 現端末購入代理店コード・名称
- 現端末購入運営店コード・名称
- 現端末購入取次店コード・名称

4. 契約プラン関連情報
- 現在プラン、現在プラン申込日
- 前回プラン終了日、前回プラン
- 現在dプラン、現在dプラン申込日
- 前回dプラン終了日、前回dプラン

5. オプションサービス加入状況
- 基本パック加入日・解除日、加入有無
- あんしん保証加入日・解除日、加入有無

6. 固定回線関連情報
- bb固定internet種別
- cat_固定契約状況
- bb_光_申込日、bb_光_契約日
- bb_air_申込日、bb_air_契約日
- bb_おうち電話_開通日
- cat_固定＋電話状況

7. 長期特典関連情報
- 長期特典権利付与日
- 長期特典初回申込日
- 長期特典申込日
- cat_長期継続特典区分

8. f#関連（グループスマホ台数など）
- f#
- f#内スマホ数
- 自分除f#内スマホ数

9. 利用頻度・接触回数（直近3ヶ月および1〜3ヶ月単位）
- 直近3ヵ月web回数、来店回数、CS利用回数、EC利用回数
- web_cnt_1m〜3m、shop_cnt_1m〜3m、call_cnt_1m〜3m

これらの項目により、契約状況、利用状況、顧客属性、チャネル接点など多面的に分析が可能です。特に利用頻度や長期特典、オプション加入状況などの情報は顧客維持やアップセル施策の検討に役立ちます。

----------------------------------------

## 実行日時: 2026-08-25 23:41:58
**質問:** 年齢別に教えて

**実行SQL:**
```sql
SELECT
  年代,
  SUM("顧客数 (サービスUU)") AS 顧客数サービスUU合計,
  SUM("顧客数 (契約者UU)") AS 顧客数契約者UU合計,
  AVG(平均年齢) AS 平均年齢平均
FROM
  pivot
GROUP BY
  年代
ORDER BY
  年代;
```

**回答:**
年齢別の顧客数（サービス利用者数）は以下の通りです。

- 20代：5人、平均年齢約25.6歳
- 30代：11人、平均年齢約34.9歳
- 40代：16人、平均年齢約44.2歳
- 50代：7人、平均年齢約55.4歳
- 60代：3人、平均年齢約61.3歳
- 70代：8人、平均年齢約74.5歳
- 80代以上：2人、平均年齢約82.5歳
- 不明：サービス利用者48人、契約者35人、平均年齢0歳（不明）

最も多いのは40代で16人、次いで30代の11人、70代の8人と続きます。20代以下や60代以上は比較的少なめですが、70代は8人と意外に多いことが特徴です。また、年齢不明の利用者が多く48人いるため、年齢情報の取得・管理の改善が必要と考えられます。全体として、中年層を中心にサービス利用が多い傾向が見られます。

----------------------------------------

## 実行日時: 2026-08-25 23:44:10
**質問:** どんな情報が取れる？

**実行SQL:**
```sql
SELECT
  'pivot' AS table_name,
  GROUP_CONCAT(name) AS columns
FROM pragma_table_info('pivot')
UNION ALL
SELECT
  'web_contact',
  GROUP_CONCAT(name)
FROM pragma_table_info('web_contact')
UNION ALL
SELECT
  'analyze',
  GROUP_CONCAT(name)
FROM pragma_table_info('analyze')
UNION ALL
SELECT
  'user_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('user_info')
UNION ALL
SELECT
  'contract_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('contract_info')
UNION ALL
SELECT
  'service_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('service_info')
UNION ALL
SELECT
  'survey_results',
  GROUP_CONCAT(name)
FROM pragma_table_info('survey_results')
UNION ALL
SELECT
  'medallia_survey',
  GROUP_CONCAT(name)
FROM pragma_table_info('medallia_survey')
UNION ALL
SELECT
  'customer_info',
  GROUP_CONCAT(name)
FROM pragma_table_info('customer_info');
```

**回答:**
当データベースから取得できる情報は、多岐にわたり、主に以下のカテゴリに分けられます。

1. 契約・サービス情報  
- 支社コード、契約プラン、サービス提供事業者  
- 契約状況（契約月数、契約日数、解約年月日、契約者ステータスなど）  
- 端末関連（端末契約フラグ、移動機型、機種名、IMEI情報）  
- プラン情報（基本パック、あんしん保証、ポイントサービス区分など）  
- 支払方法、割賦販売情報、キャンペーンコードなど  
- 家族利用状況（平均家族利用回線数、家族割引フラグなど）  

2. 顧客・ユーザー情報  
- 性別、年齢、年代、職業、ビジネス利用度  
- 契約者・使用者の識別情報（識別ハッシュ値）  
- 住所情報（都道府県、市区町村コード）  
- 契約者タイプや外国人フラグなどの属性情報  

3. 利用状況・行動データ  
- Web利用回数、来店回数、カスタマーサービス利用回数、EC利用回数  
- Webアクセスログ（ページURL、イベントタイプ、アクセス日時、デバイスIDなど）  

4. アンケート・調査データ  
- 顧客満足度やサービス評価に関する詳細なアンケート回答結果（設問別回答）  
- アンケート送信状況、回答日時、回答所要時間  

5. 代理店・店舗情報  
- 代理店コード、店舗コード、取次店情報、チャネル情報  
- 契約時や解約時の代理店情報、担当者コード  

6. その他  
- MNP（番号ポータビリティ）関連情報  
- 各種フラグ（滞納、盗難・紛失、一時停止など）  
- 各種サービス開始・解除日、割引適用状況  

以上の情報により、顧客の契約状況から利用行動、満足度調査まで幅広く把握可能です。例えば、契約プラン別の平均契約月数や顧客数、年代別や地域別の利用傾向分析、Webアクセスの動向解析、アンケート結果の詳細な評価分析など、多角的なデータ分析に活用できます。

----------------------------------------

## 実行日時: 2026-08-25 23:47:12
**質問:** これからどういうことが分析できそう？

**実行SQL:**
```sql
SELECT
  p."支社コード",
  p."年代",
  p."契約プラン",
  p."サービス提供事業者",
  AVG(p."平均年齢") AS "平均年齢平均",
  AVG(p."平均契約月数") AS "平均契約月数平均",
  SUM(p."顧客数 (サービスUU)") AS "総サービスUU",
  SUM(p."顧客数 (契約者UU)") AS "総契約者UU",
  AVG(a."契約月数") AS "契約月数平均",
  AVG(a."契約年数") AS "契約年数平均",
  AVG(u."年齢") AS "利用者年齢平均",
  COUNT(DISTINCT sr."unique_id") AS "アンケート回答数",
  AVG(sr."回答結果1_回答内容") AS "アンケート回答1平均",
  AVG(sr."回答結果7_回答内容") AS "アンケート回答7平均"
FROM
  pivot p
LEFT JOIN
  analyze a ON p."サービス提供事業者" = a."サービスid"
LEFT JOIN
  user_info u ON a."サービスid" = u."サービスid"
LEFT JOIN
  survey_results sr ON a."サービスid" = sr."サービスid"
GROUP BY
  p."支社コード",
  p."年代",
  p."契約プラン",
  p."サービス提供事業者"
ORDER BY
  p."支社コード",
  p."年代",
  p."契約プラン",
  p."サービス提供事業者";
```

**回答:**
結論として、現在のデータからは「年代別や支社別の契約プランごとの利用者分布」や「契約期間の平均値」が一部把握できるため、今後はこれらの属性を活用した顧客層分析や、契約プラン・支社ごとの利用傾向分析が可能です。

具体的には以下のような分析が考えられます。

1. **年代別の契約プラン利用状況の分析**  
　例：20代、30代、40代、70代など年代ごとにどの契約プランが多いかを把握し、若年層は「基本プラン（音声）」や「その他」の契約が多い傾向などを探れます。

2. **支社別の顧客分布・利用傾向の把握**  
　支社コードごとに利用者数（総契約者UU、総サービスUU）が異なり、例えば支社「03」では総契約者が多く計33件のサービス利用が確認できるため、地域別の需要傾向を把握しやすいです。

3. **契約期間の平均値（契約月数平均）による継続性の分析**  
　現在のデータでは契約月数平均が主に1.0前後であり、短期間の契約が多い可能性があります。この点を深掘りし、長期契約者の割合や解約傾向を分析することで顧客維持策に繋げられます。

4. **年代別の平均年齢や契約者数の推移分析**  
　年代ごとの平均年齢は概ねデータ通りで、例えば40代で平均年齢約42～48歳、70代で約70～84歳などが確認でき、年齢層ごとの契約傾向を把握できます。

なお、アンケート回答がゼロであるため、顧客満足度やニーズの把握は現状困難であり、アンケート収集強化が今後の課題です。

以上を踏まえ、より詳細な傾向分析には契約年数やアンケート回答などの追加データ取得が望まれますが、現時点でも年代・支社・契約プラン単位での利用状況把握や契約継続性の分析が可能です。

----------------------------------------

